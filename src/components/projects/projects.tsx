import { PROJECTS } from "@/config/globals";
import styles from "./projects.module.scss";
import Section from "../section/section";

export default function Projects() {
  return (
    <Section
      id={PROJECTS}
      header="Projects"
      text="Things I've developed in my own time"
    >
      <></>
    </Section>
  );
}
